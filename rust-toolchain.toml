[toolchain]
channel = "stable"
profile = "minimal"
components = ["rust-analyzer", "rustfmt"]
targets = [
    # Linux Targets
    "x86_64-unknown-linux-gnu",   # For host build scripts & dynamic builds
    "x86_64-unknown-linux-musl",  # For static Linux builds
    # "x86_64-unknown-linux-musl",
    # "aarch64-unknown-linux-gnu",
    # "aarch64-unknown-linux-musl",
    # Apple Targets
    # "x86_64-apple-darwin",
    "aarch64-apple-darwin",
    # Windows
    # "x86_64-pc-windows-gnu",
]
